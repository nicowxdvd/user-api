= Revisión de código — registro público y validación de roles
:toc: left
:toclevels: 2
:sectnums:
:icons: font
:source-highlighter: highlight.js

[cols="1,3"]
|===
| Fecha | 2026-09-12
| Rama | `develop`
| Rango revisado | `25a3f06..61a3852`
| Estado | 2 resueltas, 15 abiertas
|===

Cada incidencia tiene una casilla para ir tachándolas de a una. El orden es por
severidad: primero lo que bloquea, después lo importante, al final la deuda que
ya existía antes de estos commits.

Las incidencias `INC-01` a `INC-05` salen de la revisión del diff. De `INC-06` en
adelante son observaciones detectadas durante la sesión que viven fuera de esos
commits, pero que afectan a los mismos endpoints.

== Resueltas

=== [x] INC-01 — Escalada de privilegios en el registro público

[cols="1,3"]
|===
| Severidad | *ALTA*
| Archivo | `src/users/users.controller.ts`, `src/users/dto/create-user.dto.ts`
| Estado | *Resuelta* en `de5476c`
|===

`POST /users` quedó público, pero `CreateUserDto` aceptaba un `roleId` elegido por
quien llamaba. Un anónimo podía leer `GET /roles`, tomar el id de ADMIN y
registrarse como administrador. Se confirmó en la práctica: durante la sesión se
crearon tres usuarios con rol ADMIN sin enviar token.

*Solución aplicada:* se eliminó `roleId` de `CreateUserDto`. El campo ya no se acepta
en el cuerpo, y el `forbidNonWhitelisted` del `ValidationPipe` global devuelve 400 si
alguien lo manda. `UsersService.create` asigna el rol por su cuenta.

Verificado: registro sin token ni `roleId` responde 201 con el rol fijo; enviando
`roleId` responde 400. Ver `INC-17` por la deuda temporal que dejó la solución.

=== [x] INC-04 — Los roles desactivados quedaban invisibles

[cols="1,3"]
|===
| Severidad | *MEDIA*
| Archivo | `src/roles/roles.controller.ts`
| Estado | *Resuelta* en `61a3852`
|===

`GET /roles` se había abierto al público y se había limitado a roles activos, con lo
que un rol desactivado desaparecía del único listado existente y quedaba sin forma de
recuperar su id para reactivarlo.

*Solución aplicada:* al fijar el servidor el rol de creación, el formulario de registro
dejó de necesitar la lista de roles, así que el endpoint volvió a exigir token y se
restauró el parámetro `isActive`.

Verificado: sin token responde 401; con token devuelve los tres roles; con
`?isActive=false` devuelve el rol inactivo.

== Bloqueantes

=== [ ] INC-08 — `RolesGuard` no aplica ninguna restricción

[cols="1,3"]
|===
| Severidad | *ALTA*
| Archivo | `src/common/decorators/guards/roles.guard.ts`
|===

`canActivate` devuelve `true` siempre y hace `console.log` de la request completa.
El decorador `@Roles('ADMIN')` escribe la metadata `'roles'`, pero nadie la lee.
Todas las restricciones por rol del proyecto son decorativas.

Bloquea `INC-02` e `INC-03`: ninguna de las dos se puede cerrar mientras el guard no
exista de verdad.

El patrón a seguir ya está en el repositorio: `AuthGuard` lee la metadata con
`reflector.getAllAndOverride`, igual que hace con `@Public()`.

== Importantes

=== [ ] INC-02 — El guard de ADMIN se movió de `create` a `findOne`

[cols="1,3"]
|===
| Severidad | *MEDIA*
| Archivo | `src/users/users.controller.ts:46`
| Commit | `53835a1`
|===

El commit que abrió el registro también eliminó `@UseGuards(AuthGuard, RolesGuard)`
y `@Roles('ADMIN')` de `create` y los agregó sobre `@Get(':id')`. Ese movimiento no
tiene relación con el objetivo declarado del commit.

Verificado contra el repositorio: tanto `95d56a8` como `origin/develop` tienen esos
decoradores sobre `create`, con `findOne` sin ninguno.

Dos consecuencias:

. No protege nada, por `INC-08`.
. Filtra el token en los logs. `RolesGuard` hace `console.log(request)` sobre la
  request cruda de Express. `headers.authorization` queda a profundidad 2, que es la
  que imprime `util.inspect` por defecto, así que cada lectura de perfil vuelca el
  bearer del usuario al log.

Además reaplica `AuthGuard` de forma redundante, porque ya está a nivel de clase.

*Nota de proceso:* el cambio entró porque se hizo `git add` del archivo completo sin
revisar el diff antes de commitear. Conviene revisar `git diff --staged` antes de
cada commit.

=== [ ] INC-03 — Cualquiera que se registre puede leer el directorio completo

[cols="1,3"]
|===
| Severidad | *MEDIA*
| Archivo | `src/users/users.controller.ts:38`
|===

`GET /users` pide token válido, pero ninguna restricción por rol. Como el registro es
público, cualquier persona puede crear una cuenta descartable, loguearse y volcar el
correo, nombre, `roleId` y fechas de todos los usuarios.

`INC-01` no cambia esto: el atacante ya no obtiene rol ADMIN, pero un rol básico
alcanza para leer el directorio. Depende de `INC-08`.

=== [ ] INC-06 — `User.isActive` es `varchar` pero está tipado como `boolean`

[cols="1,3"]
|===
| Severidad | *MEDIA*
| Archivo | `src/users/entities/user.entity.ts`, `src/auth/auth.service.ts:36`
|===

La columna se declara `{ type: 'varchar' }` aunque la propiedad sea `boolean`.
`AuthService.login` hace `if (!user.isActive)` para rechazar usuarios inactivos. Un
`"0"` de MySQL es una cadena no vacía, o sea *truthy*, así que un usuario inactivo
pasaría el chequeo y podría iniciar sesión.

Sin comprobar contra la base: es deducción a partir del tipo de columna. Conviene
verificarlo con un usuario inactivo real antes de tocar nada.

NOTE: `Role.isActive` sí es `{ type: 'boolean' }`. Se verificó durante la revisión.

=== [ ] INC-07 — `UsersService.remove` no borra nada

[cols="1,3"]
|===
| Severidad | *MEDIA*
| Archivo | `src/users/users.service.ts`
|===

Devuelve el string `This action removes a #${id} user` sin tocar la base. El endpoint
`DELETE /users/:id` responde como si hubiera funcionado.

Además el mensaje está en inglés, contra la convención del proyecto.

=== [ ] INC-14 — `PATCH /users/:id` exige todos los campos

[cols="1,3"]
|===
| Severidad | *MEDIA*
| Archivo | `src/users/dto/create-user.dto.ts`, `src/users/dto/update-user.dto.ts`
|===

Un `PATCH` con `{"firstName":"SoloNombre"}` responde 400 reclamando apellido y correo.
`UpdateUserDto` usa `PartialType`, pero los campos de `CreateUserDto` tienen
inicializador (`firstName: string = ''`). Con `transform: true`, `class-transformer`
materializa esos valores por defecto, así que el campo llega como cadena vacía y
`@IsNotEmpty` dispara. `@IsOptional` solo omite valores `undefined` o `null`.

Resultado: el `PATCH` solo funciona con el cuerpo completo, o sea que no es un `PATCH`.

*Cómo se arregla:* quitar los inicializadores de los DTOs y declarar las propiedades
con `!` o como opcionales.

== Menores

=== [ ] INC-05 — `auth.guard.spec.ts` no compila

[cols="1,3"]
|===
| Severidad | *BAJA*
| Archivo | `src/auth/auth.guard.spec.ts:5`
|===

`AuthGuard` pasó a recibir dos argumentos (`JwtService` y `Reflector`), pero el spec
sigue haciendo `new AuthGuard()`.

[source]
----
src/auth/auth.guard.spec.ts(5,12): error TS2554: Expected 2 arguments, but got 0.
----

No es una regresión: en `main` el constructor ya pedía un argumento obligatorio, así
que la llamada era igual de inválida. `npm run build` no lo ve porque
`tsconfig.build.json` excluye los specs.

=== [ ] INC-09 — Cuatro suites de test rotas

[cols="1,3"]
|===
| Severidad | *BAJA*
| Archivos | `users.service.spec.ts`, `users.controller.spec.ts`, `roles.service.spec.ts`, `roles.controller.spec.ts`
|===

Las cuatro son scaffolding del CLI de Nest sin modificar, con `providers: [XService]`
pelado y sin mock del token de repositorio. Fallan al resolver dependencias.

=== [ ] INC-10 — `main.ts` ignora la variable `PORT`

[cols="1,3"]
|===
| Severidad | *BAJA*
| Archivo | `src/main.ts`
|===

`.env` declara `PORT=3000` pero el código hace `app.listen(3001)` fijo. Cuidado al
corregirlo: el puerto 3000 lo ocupa un servidor Next.js en esta máquina, así que leer
`PORT` tal como está hoy haría chocar la API.

=== [ ] INC-11 — Archivos de herramientas versionados

[cols="1,3"]
|===
| Severidad | *BAJA*
| Archivos | `.claude/.headroom_wrap_marker.json`, `.serena/project.yml`
|===

El primero solo guarda un PID que cambia en cada sesión, así que ensucia
`git status` permanentemente. Van al `.gitignore` con `git rm --cached`.

=== [ ] INC-12 — Formato inconsistente en todo el repositorio

[cols="1,3"]
|===
| Severidad | *BAJA*
|===

`npx prettier --check src/` falla en 47 archivos. Mientras siga así, el formato no
sirve como señal en las revisiones.

=== [ ] INC-13 — `CLAUDE.md` desactualizado

[cols="1,3"]
|===
| Severidad | *BAJA*
| Archivo | `CLAUDE.md`
|===

Describe `AuthService.login` con credenciales fijas (`nico` / `_nico_123`) y el
secreto JWT incrustado en `auth.module.ts`. Ambas cosas ya están resueltas. Falta
documentar el decorador `@Public()`, la regla del rol fijo y el módulo
`src/user-profiles/`, que no aparece.

=== [ ] INC-15 — Los mensajes del `ValidationPipe` salen en inglés

[cols="1,3"]
|===
| Severidad | *BAJA*
| Archivo | `src/main.ts`
|===

Enviar un campo no declarado devuelve `"property roleId should not exist"`. El mensaje
lo genera el `ValidationPipe`, no un DTO, así que no lo cubren los `message` en español
de las validaciones. El `CLAUDE.md` exige que toda respuesta al usuario esté en
español.

*Cómo se arregla:* pasar un `exceptionFactory` al `ValidationPipe` global que traduzca
o reemplace esos mensajes.

=== [ ] INC-16 — Seis errores de lint en `user-profiles.service.ts`

[cols="1,3"]
|===
| Severidad | *BAJA*
| Archivo | `src/user-profiles/user-profiles.service.ts`
|===

Seis errores `@typescript-eslint/no-unsafe-member-access` en las líneas 28, 79 y 99,
por el acceso a `error.code` y `error.errno`. Es el mismo patrón que usan
`users.service.ts` y `roles.service.ts`, pero a este archivo le falta el
`eslint-disable` de cabecera que los otros dos sí tienen.

Son los únicos errores de lint del proyecto.

=== [ ] INC-17 — El id del rol por defecto está cableado en el código

[cols="1,3"]
|===
| Severidad | *BAJA*
| Archivo | `src/users/users.service.ts`
| Commit | `de5476c`
|===

`UsersService.create` asigna `ROL_POR_DEFECTO_ID = 11`. Es una solución temporal
acordada, a la espera de que la columna `role_id` reciba su propio `DEFAULT` en la
base de datos.

Dos cosas a tener presentes cuando llegue esa tarea:

. El 11 es el id que tomó el rol `USUARIO` en esta base por autoincremento. En otra
  base el mismo rol puede tener otro id, y la aplicación crearía usuarios apuntando a
  un rol equivocado sin avisar.
. `synchronize: true` deriva el esquema de las entidades en cada arranque, así que un
  `DEFAULT` puesto con `ALTER TABLE` a mano no sobrevive. Tiene que vivir en el
  decorador `@Column` de `User.roleId`.

== Resumen

[cols="1,4,1,1"]
|===
| ID | Incidencia | Severidad | Estado

| INC-01 | Escalada de privilegios en el registro público | ALTA | Resuelta
| INC-04 | Roles desactivados quedaban invisibles | MEDIA | Resuelta
| INC-08 | `RolesGuard` no restringe nada | ALTA | Abierta
| INC-02 | Guard de ADMIN movido a `findOne` y token en los logs | MEDIA | Abierta
| INC-03 | Directorio de usuarios legible tras auto-registro | MEDIA | Abierta
| INC-06 | `User.isActive` es `varchar` tipado `boolean` | MEDIA | Abierta
| INC-07 | `UsersService.remove` no borra | MEDIA | Abierta
| INC-14 | `PATCH /users/:id` exige todos los campos | MEDIA | Abierta
| INC-05 | `auth.guard.spec.ts` no compila | BAJA | Abierta
| INC-09 | Cuatro suites de test rotas | BAJA | Abierta
| INC-10 | `main.ts` ignora `PORT` | BAJA | Abierta
| INC-11 | Archivos de herramientas versionados | BAJA | Abierta
| INC-12 | Formato inconsistente | BAJA | Abierta
| INC-13 | `CLAUDE.md` desactualizado | BAJA | Abierta
| INC-15 | Mensajes del `ValidationPipe` en inglés | BAJA | Abierta
| INC-16 | Seis errores de lint en `user-profiles.service.ts` | BAJA | Abierta
| INC-17 | Id del rol por defecto cableado | BAJA | Abierta
|===

=== Orden sugerido

`INC-08` primero: sin un `RolesGuard` real, `INC-02` e `INC-03` no tienen arreglo
posible, y entre las dos dejan el directorio de usuarios abierto y el token en los
logs. Después `INC-14`, que rompe una operación de uso diario. `INC-17` se cierra sola
cuando llegue la tarea de base de datos.
